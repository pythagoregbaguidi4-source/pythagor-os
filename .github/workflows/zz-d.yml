name: probe-d
on:
  workflow_dispatch:
    inputs:
      cible:
        type: choice
        default: mobile
        options: [mobile, desktop, les-deux]
jobs:
  j:
    if: contains(fromJSON('["mobile","les-deux"]'), inputs.cible)
    runs-on: ubuntu-latest
    steps:
      - run: echo ok
